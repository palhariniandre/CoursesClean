spring:
  application:
    name: CoursesClean

  datasource:
    url: jdbc:postgresql://localhost:5431/CoursesClean
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true

  flyway:
    enabled: true